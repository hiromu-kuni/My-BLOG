---
import { BookOpenTextIcon } from "lucide-react";

import { IJCharacter } from "@/components/ui/ichigojam";
import { IJCharacterCodes } from "@/lib/constants";
---

<div
  class="border-4 border-card-foreground bg-card sm:shadow-[4px_4px_0_0] not-prose"
>
  <div class="bg-accent border-b-4 border-card-foreground px-3 py-1 mb-4">
    <IJCharacter
      as="h3"
      className="text-accent-foreground text-xs"
      characterCodes="ABOUT THE AUTHOR"
    />
  </div>

  <div class="px-4 pb-4">
    <div class="flex flex-col sm:flex-row gap-4">
      <div class="shrink-0 mx-auto sm:mx-0">
        <div
          class="w-24 h-24 sm:w-32 sm:h-32 bg-primary/20 border-4 border-primary flex items-center justify-center"
        >
          <IJCharacter
            className="text-5xl sm:text-6xl"
            characterCodes={IJCharacterCodes.Cat}
          />
        </div>
      </div>

      <div class="flex-1 space-y-3">
        <div class="bg-background border-2 border-card-foreground p-2">
          <div class="font-bold text-base sm:text-lg">
            ひろむ（hiromu）
          </div>
        </div>

        <div class="bg-background border-2 border-card-foreground p-2">
          <p class="text-xs sm:text-sm leading-relaxed mt-1">
            福井県鯖江市に生まれたWebエンジニア。
            高校生の時に子供向けパソコンであるIchigoJamで
            プログラミングを初めて学ぶ。 趣味はツーリングとゲーム。
          </p>
        </div>

        <div class="flex flex-wrap gap-2">
          <span
            class="inline-block px-2 py-1 text-xs bg-primary shadow-[2px_2px_0_0] shadow-primary/50 text-primary-foreground border-2 border-primary"
          >
            ⭐ IchigoJam検定1級
          </span>
          <span
            class="inline-block px-2 py-1 text-xs bg-secondary shadow-[2px_2px_0_0] shadow-secondary/50 text-secondary-foreground border-2 border-secondary"
          >
            📚 技術書執筆
          </span>
        </div>

        <div class="bg-accent/10 border-2 border-accent p-2">
          <div
            class="flex items-center gap-1 text-accent font-bold text-xs sm:text-sm mb-1"
          >
            <BookOpenTextIcon size={16} />
            <span>書籍</span>
          </div>
          <p class="text-xs sm:text-sm font-bold">
            「FOR文と配列にチャレンジする
            <br class="sm:hidden" />
            Hana道場式プログラミング」
          </p>
          <a
            href="https://hanadojo.official.ec/items/90694010"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block text-xs text-primary hover:text-primary/80 mt-1 max-md:underline md:hover:underline"
          >
            → 購入はこちらから
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
